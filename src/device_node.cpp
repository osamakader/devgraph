#include "device_node.hpp"

// Intentionally empty: DeviceNode is a plain data holder. This translation
// unit exists so the type has a stable home and future non-inline helpers
// (e.g. serialization) have somewhere to live without touching the header.
